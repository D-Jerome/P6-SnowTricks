{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>


{% for trick in tricks %}

<div class="container col-8"></div>
    <div class="example-wrapper">
        <h1>SnowBoard Trick {{ trick.name }}! ✅</h1>
        {{ trick.description }}
        <img src="https://via.placeholder.com/200x200" alt="trick picture" class="">
        <a href="{{ path('app_trick', {'slug' : trick.slug }) }}" class="btn btn-primary">Lire la suite</a>
    </div>

</div>
{% endfor %}
{% endblock %}
